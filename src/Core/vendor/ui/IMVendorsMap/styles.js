import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  map: {
    flex: 1,
    height: '100%',
    width: '100%',
  },
  btnIcon: {
    height: 30,
    width: 30,
    elevation: 4,
    shadowRadius: 3,
  },
});

export default styles;
